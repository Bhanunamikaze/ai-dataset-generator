jsonschema>=4.26,<5

# Optional: enables syntax-tree parsing for structured C/C++/assembly ingestion
tree-sitter>=0.25,<0.26
tree-sitter-language-pack>=1.3,<2

# Optional: required only for scripts/collect.py (local web/URL/file collector)
# Install with: python3 -m pip install -r requirements.txt
requests>=2.31,<3
beautifulsoup4>=4.12,<5
trafilatura>=1.12,<2
duckduckgo-search>=6.1,<8
